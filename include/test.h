#include <vector>

using namespace std;

class Binarysearch{
public:
    int binarySearch(vector<int> &data, int);
    int binarySearchRecursion(vector<int> &data, int, int, int);
};

int arrayDefine();

int sqrtInt(int);

int moveElement(int nums[], int val, int numssize);

int removeElement(vector<int>& nums, int val);
